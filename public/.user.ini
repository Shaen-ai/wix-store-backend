; Increase upload limits for product images (up to 4 × 20MB) and GLB files (50MB).
; Required to avoid 413 Content Too Large when creating products with images.
; PHP-FPM reads this file; for mod_php use .htaccess with php_value instead.
upload_max_filesize = 100M
post_max_size = 105M
